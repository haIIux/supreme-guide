// This will house the final output of calories and the sliders. Once Calculate is pressed on SecondView, it'll dump over to this view with that data.


import SwiftUI

struct FinalView: View {
    var body: some View {
        Text(/*@START_MENU_TOKEN@*/"Hello, World!"/*@END_MENU_TOKEN@*/)
    }
}

struct FinalView_Previews: PreviewProvider {
    static var previews: some View {
        FinalView()
    }
}
